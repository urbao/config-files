# enable terminal truecolor
set -g default-terminal "screen-256color"

# change prefix of tmux, and unbind default prefix
set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

# reset split screen keybind
unbind %
bind | split-window -h
unbind '"'
bind - split-window -v

# allow shortcut to reload tmux config file
unbind r
bind r source-file ~/.tmux.conf

# allow user to resize pane by vim-style(hjkl)
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

# allow maximize the current pane
bind -r m resize-pane -Z

# enable mouse
set -g mouse on
set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

unbind -T copy-mode-vi MouseDragEnd1Pane

# add plugin manager
set -g @plugin 'tmux-plugins/tpm'

# list of tmux plugin
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'jimeh/tmux-themepack'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @themepack 'powerline/default/cyan'

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'

# initialize TMUX plugin manager(keep this line at the BOTTOM of the file)
run '~/.tmux/plugins/tpm/tpm'
